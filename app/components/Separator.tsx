import * as React from 'react';
import styled from 'styled-components/native';

const Separator: React.FC = styled.View`
  margin-vertical: 30;
  height: 1;
  width: '80%';
`;

export default Separator;
